electrumx:
  host: "192.168.1.240"
  port: 50001
  use_ssl: false
  timeout_seconds: 30
  allowed_hosts:
    - "192.168.1.240"
    - "127.0.0.1"
    - "localhost"

state_path: "/var/lib/openclaw/bitcoin_balance_monitor/state.json"

wallets:
  - label: "Coldcard Q — Main"
    descriptor_secret_name: "BTC_WATCH_MAIN_DESCRIPTOR"
    derivation_gap_limit: 20
    dust_threshold_sats: 0
